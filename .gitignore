node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# databáza a lokálne dáta
data/
*.db
*.sqlite
*.db-journal
*.db-wal
*.db-shm

# Money cenový snapshot (#154) — NIKDY sa necommituje, repo je VEREJNÉ. `data/` vyššie
# už pokrýva predvolenú cestu (`/data/ceny/ceny.json`), toto je explicitná druhá vrstva
# pre hocijaké iné umiestnenie (CENY_SNAPSHOT_PATH je nastaviteľná).
ceny.json
/ceny/

# playwright
test-results/
playwright-report/
coverage/
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
*.env.local
node_modules/
dist/
build/

# Playwright MCP browser profile/artefakty (lokálny read-back)
.playwright-mcp/

# StrykerJS mutation testing (#248) — reporty + sandbox, nikdy sa necommitujú
reports/
.stryker-tmp/
stryker.log

# Claude worktree checkouts (never commit)
.claude/worktrees/
